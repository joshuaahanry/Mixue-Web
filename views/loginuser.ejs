<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Mixue</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
    }
    .left-side {
      background-color: #e50012;
      color: white;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 2rem;
    }
    .left-side img {
      max-width: 150px;
    }
    .right-side {
      background-color: #ffffff;
      padding: 4rem 2rem;
    }
    .form-control {
      border-radius: 20px;
      box-shadow: 1px 1px 5px #ccc;
    }
    .btn-login {
      border-radius: 20px;
      background-color: black;
      color: white;
    }
    .btn-google {
      border-radius: 20px;
      background-color: white;
      box-shadow: 1px 1px 5px #ccc;
    }
    .form-icon {
      position: absolute;
      left: 15px;
      top: 50%;
      transform: translateY(-50%);
      color: gray;
    }
    .form-input {
      padding-left: 40px;
    }
    .notice {
      color: red;
      text-align: center;
      margin-top: 1rem;
    }
  </style>
</head>
<body>
  <div class="container-fluid h-100">
    <div class="row h-100">

      <!-- LEFT SIDE -->
      <div class="col-md-6 d-none d-md-flex flex-column left-side">
        <img src="img/Logo MIXUE.png" alt="Mixue Logo" style="max-width: 300px; margin-top: 2rem;">
        <img src="img/Mixue1.png" alt="Love Snow King" style="max-width: 450px; margin-top: 2rem;">
      </div>

      <!-- RIGHT SIDE -->
      <div class="col-md-6 col-12 right-side d-flex flex-column justify-content-center">
        <h2 class="text-center fw-bold text-danger">MIXUE</h2>
        <p class="text-center fw-semibold">Welcome to Mixue</p>
  

        <form class="mt-4" method="POST" action="/loginuser">
    <% if (errors && errors.length) { %>
      <% errors.forEach(error => { %>
        <p class="notice"><%= error %></p>
      <% }) %>
    <% } %>
    <div class="mb-3 position-relative">
      <span class="form-icon">✉️</span>
      <input 
        type="email" 
        class="form-control form-input" 
        placeholder="Masukkan Email" 
        name="email"
        required>
    </div>
    <div class="mb-3 position-relative">
      <span class="form-icon">🔐</span>
      <input 
        type="password" 
        class="form-control form-input" 
        placeholder="Kata sandi" 
        name="password"
        required>
    </div>
    <div class="text-end mb-3">
      <a href="#" class="text-decoration-none text-muted small">Forget Password?</a>
    </div>
    <button type="submit" class="btn btn-login w-100 mb-3">Masuk</button>
    <div class="text-center text-muted mb-2">Atau</div>

    <!-- Ubah tombol Daftar Akun jadi link dengan styling tombol -->
    <a href="/daftar" class="btn btn-google w-100 text-center d-block text-decoration-none">
      Daftar Akun
    </a>
  </form>
      </div>

    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
